{
  "name": "march-not-so-madness",
  "version": "1.0.0",
  "description": "Probabilistic Forecasting & Match Predictor for 2026 NCAA Tournaments",
  "scripts": {
    "dev": "npm run dev --prefix webapp",
    "build": "npm run build --prefix webapp",
    "start": "npm run start --prefix webapp"
  }
}
